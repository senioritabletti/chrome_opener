plugins {
    // no “com.android.application” here — that goes in :app
    // define versions only once
    id("com.android.application") version "8.13.0" apply false
    id("org.jetbrains.kotlin.android") version "2.2.21" apply false
}